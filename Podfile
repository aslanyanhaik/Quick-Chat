platform :ios, '12.0'
use_frameworks!
inhibit_all_warnings!
ENV['COCOAPODS_DISABLE_STATS'] = "true"

target 'QuickChat' do

	pod 'Firebase/Auth'
	pod 'Firebase/Storage'
	pod 'Kingfisher'
	pod "Firebase/Firestore"

end
